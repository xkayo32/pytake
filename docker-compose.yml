version: "3.9"

name: pytake-dev

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: pytake-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pytake_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-Odc7/ffNnTnG4hkbwV+Sx2ZgK61rXW2r9U2o7Rd25DU=}
      POSTGRES_DB: ${POSTGRES_DB:-pytake}
    ports:
      - "5435:5432"
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - pytake-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pytake_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: pytake-redis-dev
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-gOe7JRn+i8iWY5UAvYt3mJxBFJnAf9+jo/VZM3UN4xw=}
    ports:
      - "6382:6379"
    volumes:
      - redis_data_dev:/data
    networks:
      - pytake-network
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # MongoDB Database
  mongodb:
    image: mongo:7
    container_name: pytake-mongodb-dev
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-pytake_user}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-pytake_mongo_password}
    ports:
      - "27020:27017"
    volumes:
      - mongodb_data_dev:/data/db
    networks:
      - pytake-network

  # Python FastAPI Backend - DEVELOPMENT
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pytake-backend-dev
    restart: unless-stopped
    ports:
      - "8002:8000"
    volumes:
      - ./.env:/app/.env:ro
      - ./backend:/app
    environment:
      # Database
      DATABASE_URL: postgresql://${POSTGRES_USER:-pytake_user}:Odc7%2FffNnTnG4hkbwV%2BSx2ZgK61rXW2r9U2o7Rd25DU%3D@postgres:5432/${POSTGRES_DB:-pytake}
      POSTGRES_USER: ${POSTGRES_USER:-pytake_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-Odc7/ffNnTnG4hkbwV+Sx2ZgK61rXW2r9U2o7Rd25DU=}
      POSTGRES_SERVER: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: ${POSTGRES_DB:-pytake}

      # Redis
      REDIS_URL: redis://default:gOe7JRn%2Bi8iWY5UAvYt3mJxBFJnAf9%2Bjo%2FVZM3UN4xw%3D@redis:6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-gOe7JRn+i8iWY5UAvYt3mJxBFJnAf9+jo/VZM3UN4xw=}
      REDIS_HOST: redis
      REDIS_PORT: 6379

      # MongoDB
      MONGODB_URL: mongodb://${MONGO_USER:-pytake_user}:${MONGO_PASSWORD:-pytake_mongo_password}@mongodb:27017
      MONGODB_DB: ${MONGODB_DB:-pytake_logs}

      # Server
      PORT: 8000
      NODE_ENV: development
      ENVIRONMENT: development

      # Auth & Security
      SECRET_KEY: ${SECRET_KEY:-0lKDucCTqSt0gh1mbLsvF/d5mhXqCtEW8JfwWwbTeIk=}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-0lKDucCTqSt0gh1mbLsvF/d5mhXqCtEW8JfwWwbTeIk=}
      JWT_ALGORITHM: HS256

      # WhatsApp (tokens configured in system, not env)
      WHATSAPP_WEBHOOK_URL: ${WHATSAPP_WEBHOOK_URL:-https://api-dev.pytake.net/webhook/whatsapp}
      META_WEBHOOK_VERIFY_TOKEN: ${META_WEBHOOK_VERIFY_TOKEN:-pytake_webhook_verify_token_12345}

    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongodb:
        condition: service_started
    networks:
      - pytake-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Nginx Reverse Proxy
  nginx:
    image: nginx:1.25-alpine
    container_name: pytake-nginx-dev
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx-dev.conf:/etc/nginx/nginx.conf:ro
      - ./certbot/conf:/etc/letsencrypt:ro
      - ./certbot/www:/var/www/certbot:ro
    depends_on:
      - backend
    networks:
      - pytake-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "https://api-dev.pytake.net/nginx-health", "--no-check-certificate"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  postgres_data_dev:
    driver: local
  redis_data_dev:
    driver: local
  mongodb_data_dev:
    driver: local

networks:
  pytake-network:
    driver: bridge
