# Application
ENVIRONMENT=development
DEBUG=True
SECRET_KEY=your-super-secret-key-change-in-production-min-32-chars
APP_NAME=PyTake
APP_VERSION=1.0.0

# JWT
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# Encryption for Secrets (Fernet symmetric encryption)
ENCRYPTION_KEY=TYrqEJwoQKC00T5_fqsI-0bB-4spG4YqplQsx4VSKkw=

# Database - PostgreSQL (values from docker-compose)
POSTGRES_SERVER=postgres
POSTGRES_PORT=5432
POSTGRES_USER=pytake
POSTGRES_PASSWORD=pytake_dev_password
POSTGRES_DB=pytake_dev

# Database - Redis (values from docker-compose)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Database - MongoDB (values from docker-compose)
MONGODB_URL=mongodb://mongodb:27017
MONGODB_DB=pytake_logs

# Celery
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://127.0.0.1:3000,http://127.0.0.1:3001,http://127.0.0.1:3002,https://app.pytake.net,https://app.pytake.net.br
CORS_CREDENTIALS=True

# Testing
TESTING=False
